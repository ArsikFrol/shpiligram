'use client'

import useProfile from "@/store/profile/profileStore"

export default function Profile() {

    const {

    } = useProfile()

    return (
        <div className=''>
            asdf
        </div>
    )
}